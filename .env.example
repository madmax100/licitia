# Environment Variables Configuration Example

# Path to the input directory containing PDF files
INPUT_DIR=data/input

# Path to the output directory for storing results
OUTPUT_DIR=data/output

# Path to the Tesseract executable (if not in PATH)
TESSERACT_PATH=/usr/local/bin/tesseract

# Model configuration for Ollama AI
OLLAMA_MODEL_PATH=/path/to/ollama/model

# Any additional configuration parameters can be added below
# Example: AI_MODEL_TIMEOUT=30